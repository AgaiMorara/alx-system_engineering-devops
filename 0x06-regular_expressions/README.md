The best way to gain regular expression fluency is to practice using them every
day.. Here we match strings using a given ruby script
puts ARGV[0].scan(/127.0.0.[0-9]/).join we just do the solutions in the matching
part